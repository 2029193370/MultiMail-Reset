# 数据库（默认使用 SQLite，生产建议 PostgreSQL）
DATABASE_URL=sqlite+aiosqlite:///./mailguard.db
DATABASE_URL_SYNC=sqlite:///./mailguard.db

# 加密密钥（务必修改为随机字符串）
ENCRYPTION_KEY=change-this-to-a-32-byte-key-00

# 跨域配置
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# 调试模式
DEBUG=false
